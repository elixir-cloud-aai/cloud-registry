[request_definition]
r = sub, obj, act

[policy_definition]
p = sub, obj, act

[role_definition]
g = _, _

[policy_effect]
e = some(where (p.eft == allow))

[matchers]
m = r.sub == p.sub && g(r.obj, p.obj) && r.act == p.act


# model definitions for cloud-registry
# p, alice, maintainer, /ga4gh/registry/v1/1/services
# p, bob, owner, /ga4gh/registry/v1/1/services
# p, joe, admin, /ga4gh/registry/v1/service-info

# g, maintainer, PUT
# g, owner, DELETE
# g, owner, PUT
# g, admin, PUT

